general/code_coverage/second_client = Optional item that requires a second client implementation of EIP-8250, which does not exist yet.
opcode = EIP-8250 introduces no new opcode; it only assigns four previously undefined parameter values to the TXPARAM opcode that EIP-8141 already defines.
precompile = EIP-8250 does not introduce a precompile.
removed_precompile = EIP-8250 does not remove a precompile.
system_contract = The NONCE_MANAGER predeploy is protocol-managed storage whose code reverts on every ordinary call, so it is never invoked by a system call and has no input, output, or gas-usage surface to test as a system contract.
block_header_field = EIP-8250 adds no new block header fields.
block_body_field = EIP-8250 adds no new block body fields.
gas_refunds_changes = EIP-8250 introduces no new refund rule; the first-use gas it charges is ordinary frame gas that flows through EIP-8141's existing unpaid-gas refund unchanged.
blob_count_changes = EIP-8250 does not change blob counts or blob gas accounting.
execution_layer_request = EIP-8250 does not introduce an execution layer request.
block_level_constraint = EIP-8250 adds no block-level validation constraint; its per-key sequence check is a per-transaction stateful validity rule evaluated at each transaction's position in block order.
new_transaction_validity_constraint = EIP-8250 replaces EIP-8141's existing sender-nonce validity check rather than introducing a new constraint, so the modified_transaction_validity_constraint category applies instead.
transaction_type/test/contract_creation = Frame transactions have no top-level recipient field to omit, so contract creation is expressed by a frame rather than by the transaction type that this EIP modifies.
transaction_type/test/intrinsic_validity/to = Frame transactions have no top-level `to` field; EIP-8250 does not add one.
transaction_type/test/intrinsic_validity/value_non_zero_insufficient_balance = Frame transactions carry no top-level value field; value is transferred per frame from each frame's own gas and balance.
transaction_type/test/intrinsic_validity/value_non_zero_sufficient_balance = Frame transactions carry no top-level value field; value is transferred per frame from each frame's own gas and balance.
transaction_type/test/intrinsic_validity/value_zero_insufficient_balance = Frame transactions carry no top-level value field; value is transferred per frame from each frame's own gas and balance.
transaction_type/test/intrinsic_validity/value_zero_sufficient_balance = Frame transactions carry no top-level value field; value is transferred per frame from each frame's own gas and balance.
transaction_type/test/intrinsic_validity/gas_limit/exact = Frame transactions have no gas limit field; the limit is derived from the intrinsic cost plus the sum of the frame gas limits, so there is no user-supplied value to set to the exact intrinsic cost.
transaction_type/test/intrinsic_validity/gas_limit/insufficient = Frame transactions have no gas limit field, so a limit below the intrinsic cost cannot be encoded.
transaction_type/test/intrinsic_validity/chain_id = EIP-8250 does not change the chain_id field or its validity rule.
transaction_type/test/tx_scoped_attributes/older_tx_types = The four parameters this EIP adds are readable only from within a frame transaction, and TXPARAM itself halts outside one, so older transaction types cannot observe them.
transaction_type/test/tx_scoped_attributes/persistent/reset = The keyed-nonce parameters are transaction-scoped constants with no in-transaction reset point; nothing in the EIP can change them once the transaction begins.
transaction_type/test/fork_transition/before = The Bogota pseudo-fork used by this suite has no registered transition fork, so no fixture can place a block on either side of the EIP-8250 activation boundary.
transaction_type/test/intrinsic_validity/max_fee/max_priority_lower_than_max_fee = EIP-8250 does not change EIP-8141's fee fields or the validity rules that compare them.
transaction_type/test/intrinsic_validity/max_fee/max_priority_equal_to_max_fee = EIP-8250 does not change EIP-8141's fee fields or the validity rules that compare them.
transaction_type/test/intrinsic_validity/max_fee/base_lower = EIP-8250 does not change EIP-8141's fee fields or the validity rules that compare them.
transaction_type/test/intrinsic_validity/max_fee/base_equal = EIP-8250 does not change EIP-8141's fee fields or the validity rules that compare them.
transaction_type/test/signature/invalid/field_outside_curve = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/v/2 = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/v/27 = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/v/28 = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/v/35 = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/v/36 = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/v/max = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/r/0 = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/r/secp256k1n_minus_one = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/r/secp256k1n = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/r/secp256k1n_plus_one = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/r/max_minus_one = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/r/max = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/0 = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/secp256k1n_half_minus_one = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/secp256k1n_half = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/secp256k1n_half_plus_one = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/secp256k1n_minus_one = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/secp256k1n = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/secp256k1n_plus_one = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/max_minus_one = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/max = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/signature/invalid/s/complement = EIP-8250 leaves EIP-8141's signature list, its signature schemes, and their malleability rules unchanged, so this case belongs to the EIP-8141 suite.
transaction_type/test/block_interactions/eip7825/invalid = EIP-8250 does not change how a frame transaction's derived gas limit is compared against the EIP-7825 per-transaction cap; only the intrinsic term feeding it grows.
transaction_type/test/block_interactions/eip7825/valid = EIP-8250 does not change how a frame transaction's derived gas limit is compared against the EIP-7825 per-transaction cap; only the intrinsic term feeding it grows.
gas_cost_changes/test/fork_transition/before = The Bogota pseudo-fork used by this suite has no registered transition fork, so no fixture can place a block on either side of the EIP-8250 activation boundary.
gas_cost_changes/test/fork_transition/after = The Bogota pseudo-fork used by this suite has no registered transition fork, so no fixture can place a block on either side of the EIP-8250 activation boundary.
modified_transaction_validity_constraint/test/fork_transition/accepted_before_fork = The Bogota pseudo-fork used by this suite has no registered transition fork, so no fixture can place a block on either side of the EIP-8250 activation boundary.
modified_transaction_validity_constraint/test/fork_transition/rejected_before_fork = The Bogota pseudo-fork used by this suite has no registered transition fork, so no fixture can place a block on either side of the EIP-8250 activation boundary.
modified_transaction_validity_constraint/test/fork_transition/accepted_after_fork = The Bogota pseudo-fork used by this suite has no registered transition fork, so no fixture can place a block on either side of the EIP-8250 activation boundary.
modified_transaction_validity_constraint/test/fork_transition/rejected_after_fork = The Bogota pseudo-fork used by this suite has no registered transition fork, so no fixture can place a block on either side of the EIP-8250 activation boundary.
