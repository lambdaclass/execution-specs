precompile = EIP-7906 introduces no precompile
removed_precompile = EIP-7906 removes no precompile
system_contract = EIP-7906 introduces no system contract
transaction_type/test/intrinsic_validity/data_floor_above_intrinsic_gas_cost = The type 6 envelope has no top-level data field, so the data floor is derived from the frames' data by EIP-8141 and its boundary belongs to the EIP-8141 checklist
transaction_type = EIP-7906 adds a frame mode and three opcodes to the EIP-8141 type 6 transaction rather than introducing a transaction type of its own, so the envelope, signature and encoding items belong to the EIP-8141 checklist
block_header_field = EIP-7906 adds no block header field
block_body_field = EIP-7906 adds no block body field
gas_refunds_changes = EIP-7906 introduces no gas refund, and an assertion frame is static so it can trigger none
blob_count_changes = EIP-7906 changes no blob count parameter; it only reports the blob part of the escrow the transaction was already charged
execution_layer_request = EIP-7906 introduces no execution layer request
block_level_constraint = EIP-7906 introduces no block-level validation constraint; every rule it adds is checked within a single transaction
new_transaction_validity_constraint/test/fork_transition = The pseudo-fork carrying EIP-7906 has no transition variant, so no block can straddle its activation boundary and no fork-transition arm can be filled
modified_transaction_validity_constraint/test/fork_transition = The pseudo-fork carrying EIP-7906 has no transition variant, so no block can straddle its activation boundary and no fork-transition arm can be filled
opcode/test/stack_overflow = None of the three opcodes can grow the stack: TXTRACE pops two items and pushes one, TXDIFF pops three and pushes one, and EVENTDATACOPY pops four and pushes none, so no sequence of them reaches the stack limit
opcode/test/data_portion = None of the three opcodes carries a data portion; every operand is taken from the stack
opcode/test/stack_complex_operations/data_portion_variables = None of the three opcodes carries a data portion, so it has no data-portion variable to place at any stack position
opcode/test/contract_creation = None of the three opcodes creates a contract, and an assertion frame is static so it cannot create one at all
opcode/test/terminating = None of the three opcodes terminates execution; a failing one exceptionally halts, which the exceptional abort and body rollback tests already cover
opcode/test/fork_transition/at = The pseudo-fork carrying EIP-7906 has no transition variant, so no block can contain the activation boundary itself
opcode/test/execution_context/initcode/reentry = The opcodes are undefined outside a POST_TX frame and initcode can never run inside one, so there is no initcode behavior for a second entry to repeat
