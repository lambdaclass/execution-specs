precompile = EIP-8272 does not introduce a precompile.
removed_precompile = EIP-8272 does not remove a precompile.
block_header_field = EIP-8272 consumes EIP-7843's slotNumber header field and adds no header field of its own.
block_body_field = EIP-8272 adds no block body field.
blob_count_changes = EIP-8272 does not change the blob count or any blob-related limit.
execution_layer_request = EIP-8272 introduces no execution layer request.
gas_refunds_changes = EIP-8272 introduces no gas refund and changes none, so the refund calculation, its exceptional-abort behaviour and its cross-functional interaction with calldata cost are all unchanged by this EIP.
opcode/test/mem_exp = RECENTROOTREFLOAD reads no memory and writes none, so no operand of it can expand memory.
opcode/test/gas_usage/memory_expansion = RECENTROOTREFLOAD does not touch memory, so it has no memory-expansion component to its cost.
opcode/test/gas_usage/out_of_gas_memory = RECENTROOTREFLOAD does not touch memory, so it cannot run out of gas paying for memory.
opcode/test/gas_usage/extra_gas = RECENTROOTREFLOAD costs a flat RECENTROOTREFLOAD_GAS with no conditional component, so there is no extra charge to isolate.
opcode/test/gas_usage/order_of_operations = RECENTROOTREFLOAD charges one flat cost and performs no second gas-consuming step, so its charges cannot be ordered against each other.
opcode/test/stack_complex_operations/data_portion_variables = RECENTROOTREFLOAD has no data portion; both operands come from the stack.
opcode/test/stack_complex_operations/stack_heights = RECENTROOTREFLOAD pops a fixed two operands and has no data portion, so nothing in its behaviour varies with the height of the stack beneath them; which of the two it pops first is pinned by test_recentrootrefload_stack_order.
opcode/test/data_portion = RECENTROOTREFLOAD has no data portion; both operands come from the stack.
opcode/test/return_data = RECENTROOTREFLOAD pushes its result on the stack and writes no return data buffer.
opcode/test/terminating = RECENTROOTREFLOAD is not a terminating instruction: it either pushes a word or halts exceptionally, and its exceptional halts are covered by test_recentrootrefload_out_of_range_operands_halt and test_recentrootrefload_stack_underflow.
opcode/test/contract_creation = RECENTROOTREFLOAD creates no account and has no initcode form.
opcode/test/execution_context/delegatecall = RECENTROOTREFLOAD reads the enclosing transaction's signed envelope, which no call frame can change, so it returns the same word whatever account the code runs as; test_recentrootrefload_reads_envelope_not_storage pins that the source is the envelope rather than any account's state.
opcode/test/execution_context/callcode = RECENTROOTREFLOAD reads the enclosing transaction's signed envelope, which CALLCODE does not alter.
opcode/test/execution_context/initcode = RECENTROOTREFLOAD reads the enclosing transaction's signed envelope, which initcode does not alter, and a frame transaction has no transaction-level initcode form.
opcode/test/execution_context/set_code = RECENTROOTREFLOAD reads the enclosing transaction's signed envelope, which a delegation designation does not alter.
opcode/test/execution_context/tx_context = The reference set is itself a transaction-scoped attribute; test_reference_set_is_immutable_across_frames shows it reads the same in every frame of one transaction, and test_prewarm_does_not_leak_to_next_transaction that a later transaction sees its own.
opcode/test/execution_context/block_context = EIP-8272 defines RECENTROOTREFLOAD over transaction-scoped data only; the block-scoped input it consumes is EIP-7843's slot number, covered in test_current_slot_eip7843.py.
opcode/test/fork_transition = Bogota is not reachable through a transition fork in this repository, so no fixture can place a block on either side of its activation.
system_contract/test/call_contexts = RECENT_ROOT_CODE is still TBD in the specification and the reference implementation installs no code at RECENT_ROOT_ADDRESS, so no call into the contract can be exercised in any context.
system_contract/test/inputs = The contract's input format belongs to the write path, which RECENT_ROOT_CODE leaves undefined.
system_contract/test/input_lengths = The contract's input format belongs to the write path, which RECENT_ROOT_CODE leaves undefined.
system_contract/test/value_transfer = The write operation is only reachable through RECENT_ROOT_CODE, which the specification still lists as TBD, so no fixture can exercise a call carrying value.
system_contract/test/out_of_bounds = The contract's input bounds belong to the write path, which RECENT_ROOT_CODE leaves undefined.
system_contract/test/gas_usage = The contract's execution cost belongs to the write path, which RECENT_ROOT_CODE leaves undefined.
system_contract/test/excessive_gas = EIP-8272 defines no system call: the contract is written by ordinary transactions, so no block-level system-call gas budget applies.
system_contract/test/deployment = EIP-8272 creates the recent root contract at activation rather than through a deployment transaction, prescribes no deployer, and leaves the deployed code TBD.
system_contract/test/contract_variations/networks = EIP-8272 fixes RECENT_ROOT_ADDRESS at a single value for every network and defines no per-network variation.
system_contract/test/fork_transition/call_before_fork = Bogota is not reachable through a transition fork in this repository, so no fixture can place a call before its activation.
transaction_type/test/contract_creation = A frame transaction has no `to` field and cannot create a contract at the transaction level, so EIP-8272's addition to its payload has no contract-creation form.
transaction_type/test/intrinsic_validity/gas_limit = A frame transaction has no gas limit field: its limit is derived from the intrinsic cost and the frame gas limits, so an insufficient-gas-limit transaction cannot be expressed. The equivalent boundary is tested against the EIP-7825 cap in test_reference_intrinsic_gas_boundary.
transaction_type/test/intrinsic_validity/to = A frame transaction has no `to` field.
transaction_type/test/intrinsic_validity/value_non_zero_insufficient_balance = A frame transaction has no transaction-level value field; value is transferred per frame from each frame's own gas and belongs to EIP-8141.
transaction_type/test/intrinsic_validity/value_non_zero_sufficient_balance = A frame transaction has no transaction-level value field; value is transferred per frame from each frame's own gas and belongs to EIP-8141.
transaction_type/test/intrinsic_validity/value_zero_insufficient_balance = A frame transaction has no transaction-level value field; value is transferred per frame from each frame's own gas and belongs to EIP-8141.
transaction_type/test/intrinsic_validity/value_zero_sufficient_balance = A frame transaction has no transaction-level value field; value is transferred per frame from each frame's own gas and belongs to EIP-8141.
transaction_type/test/out_of_bounds/max = The only field EIP-8272 adds with a numeric bound is `slot`, and its maximum representable value can never be accepted: a reference must name a slot strictly before the block's, so accepting 2**64-1 would need a block in a later slot than any that can exist.
transaction_type/test/fork_transition/before = Bogota is not reachable through a transition fork in this repository, so no fixture can place a frame transaction before its activation.
modified_transaction_validity_constraint/test/fork_transition = Bogota is not reachable through a transition fork in this repository, so no fixture can place a transaction on either side of the payload arity change.
block_level_constraint/test/boundary = EIP-8272's block-level rule is that a block containing an invalid transaction is invalid, which is a categorical rule with no numeric boundary of its own; the reference count boundary it does define is a transaction-level one, covered by test_reference_count_boundary.
block_level_constraint/test/content = EIP-8272 constrains neither the logs, the receipts, the withdrawals nor the mix of transaction types a block may carry.
block_level_constraint/test/fork_transition = Bogota is not reachable through a transition fork in this repository, so no fixture can place a block on either side of its activation.
gas_cost_changes/test/fork_transition = Bogota is not reachable through a transition fork in this repository, so no fixture can price the same transaction on either side of its activation.
general/code_coverage/second_client = Optional in the template, and the second client's coverage is not measurable from this repository.
opcode/test/stack_overflow = RECENTROOTREFLOAD pushes one word while popping two, so it lowers the stack height and cannot overflow it.
